const HOSTNAME = "https://api.github.com/";
export const SEARCH_URL = `${HOSTNAME}search/issues?q=repo:angular/angular+type:issue`;
export const REPO_ISSUES_URL = `${HOSTNAME}repos/angular/angular/issues/{id}`;
export const REPO_URL = `${HOSTNAME}repos/angular/angular`;
